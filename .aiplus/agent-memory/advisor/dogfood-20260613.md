# AiPlus-Native Dogfood #1 — Advisor Persona 验证

**日期**: 2026-06-13
**运行时**: OpenCode (AiPlus-Native fork, branch feat/aiplus-layer-v0)
**Persona**: aiplus-advisor

## 验证点

### ✅ Persona 加载
- 命令: `opencode run --agent aiplus-advisor "review PR #1 permissions"`
- 结果: 输出 `aiplus-advisor · deepseek-v4-pro` — 身份正确加载
- Persona identity 正确: 自称 Advisor，提及 AiPlus agent team 角色

### ⚠️ 权限格式
- 初始格式 (map): `permission: { bash: { pattern: "*", action: deny } }` — **无效**
- OpenCode 要求格式 (array): `permission: [{ permission: "bash", pattern: "*", action: deny }]`
- **已修正**: 所有 5 个 persona 的 YAML frontmatter 权限改为数组格式
- 修正后 advisor 应被限制: deny bash/write/edit/task/todowrite

### ⚠️ 发现: Agent List 未显示 AiPlus persona
- `opencode agent list` 未列出 `aiplus-*` agent
- 但 `opencode run --agent aiplus-advisor` **可以**加载
- 根因: agent list 可能只扫描全局 config，不扫描项目 `.opencode/`
- 不影响功能——run 路径正常

### 📝 回复格式
- 待下次 dogfood 验证（需 Owner 交互式 session 确认 Format C 输出）

## 结论

Persona injection 可行，但需要两个修正:
1. YAML 权限格式改为 OpenCode 数组格式（已修正）
2. Agent list 发现机制待完善（低优，run 路径已通）
